<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.idn.materipertama">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Browser" />
        <activity android:name=".activities.TextToSpeech" />
        <activity android:name=".activities.WifiDevice" />
        <activity android:name=".activities.EmailClient" />
        <activity android:name=".activities.AudioManager" />
        <activity android:name=".activities.CallPhone" />
        <activity android:name=".activities.AudioRecord" />
        <activity android:name=".activities.VideoDevice" />
        <activity android:name=".activities.CameraDevice" />
        <activity android:name=".activities.SmsDevice" />
        <activity android:name=".activities.BluetoothDevice" />
        <activity android:name=".activities.Alarm"></activity>
    </application>

</manifest>